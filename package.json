{
  "name": "allowance-spending-dapp",
  "private": true,
  "workspaces": ["hardhat", "backend", "frontend"],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "test": "npm run test --workspace hardhat",
    "compile": "npm run compile --workspace hardhat",
    "deploy:testnet": "npm run deploy:testnet --workspace hardhat",
    "verify": "npm run verify --workspace hardhat"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}